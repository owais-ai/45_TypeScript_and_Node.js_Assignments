let famousPerson:string = "Colin Powell"
let message:string = (`${famousPerson} said, "There are no secrets to success. It is the result of preparation, hard work, and learning from failures"`)
console.log(message)