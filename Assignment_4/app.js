console.log("Colin Powell said, \"There are no Secrets to success. It is the result of preparation, hard work, and learning from failures.\"");
// Template literals in JavaScript are a way to define strings that allow for easy string interpolation, multi-line strings, and the inclusion of special characters without needing to escape them. They are defined using backticks (`) instead of single or double quotes.
